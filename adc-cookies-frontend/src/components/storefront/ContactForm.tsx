'use client';
import EnquiryForm from './EnquiryForm';

/** General contact form — the `contact` variant of the shared enquiry form. */
export default function ContactForm() {
  return <EnquiryForm variant="contact" />;
}
